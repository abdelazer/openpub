#summary List of extensions to core spec and links to documentation and implementationnotes.

= Introduction =

This is a list of recognised extensions to the core catalog specification.

|| *Extension* || *Brief desciption* || *Documentation* || *Implemented by* || *Primary contact* ||
|| Example: Comment feed || For books only || (link here) || Feedbooks || Hadrien Gardeur || 